stnt_id <- 'id23354'
stnt_id <- c(stnt_id, 'id11456')
stnt_id
stnt_year <- 2001
stnt_year <- c(stnt_year, 4)
stnt_year
stnt_score <- 2
cl_stnt <- c("Cho1", "Kim1", "Lee1", "Park1", "Kim2", "Kim3", NA ,"Park2")
cl_stnt
cl_stnt <- c(cl_stnt[-3])
cl_stnt
cl_stnt <- c(cl_stnt, NA, NA)
cl_stnt
combined <- c(cl_stnt[1], stnt_year[1], stnt_score[1], stnt_id[1])
combined
